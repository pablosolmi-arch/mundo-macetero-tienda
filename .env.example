DATABASE_URL=
BLOB_READ_WRITE_TOKEN=
SITE_URL=
# Shared secret for the daily Supabase keep-alive cron (/api/cron/keepalive).
# Vercel Cron sends it as `Authorization: Bearer $CRON_SECRET`.
CRON_SECRET=
